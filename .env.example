# Backend configuration
LONGQ_ROOT=./data
# Set to 1 to keep data between launches (defaults to 0: always wipe).
# LONGQ_PERSIST=0
# Optional: uncomment to pin a specific API token. Otherwise the backend auto-generates one.
# LONGQ_API_TOKEN=
ALLOWED_ORIGINS=http://127.0.0.1:5173,http://localhost:5173
MAX_UPLOAD_BYTES=2097152
EXIT_WHEN_IDLE=false
EXIT_IDLE_DEBOUNCE_SEC=20

# Frontend defaults
VITE_API_BASE=http://127.0.0.1:8000
VITE_LONGQ_API_TOKEN=
